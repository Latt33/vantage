{
  "name": "@hackathon/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "lint": "eslint api --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .vercel"
  },
  "dependencies": {
    "@vercel/node": "^3.2.0",
    "@hackathon/shared": "workspace:*",
    "@hackathon/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
